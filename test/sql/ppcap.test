# name: test/sql/ppcap.test
# description: test ppcap extension
# group: [ppcap]

# Before we load the extension, this will fail
statement error
SELECT ppcap('Sam');
----
Catalog Error: Scalar Function with name ppcap does not exist!

# Require statement will ensure this test is run with this extension loaded
require ppcap

# Confirm the extension works
query I
SELECT ppcap('Sam');
----
Ppcap Sam 🐥

query I
SELECT ppcap_openssl_version('Michael');
----
<REGEX>:Ppcap Michael, my linked OpenSSL version is OpenSSL.*